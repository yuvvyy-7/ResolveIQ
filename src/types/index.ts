/**
 * Shared TypeScript types for ResolveIQ.
 *
 * This file is the single source of truth for types shared across
 * the frontend, API routes, and agent logic.
 *
 * Types are added here phase by phase:
 *   Phase 2 → database model types (Customer, Order, Payment, Ticket, Policy)
 *   Phase 4 → Gemini/agent types (ToolCall, AgentResult, etc.)
 *   Phase 9 → UI-specific prop types
 *
 * Keep this file organised by domain, not by layer.
 */

// ─── Placeholder ────────────────────────────────────────────────────────────
// This file will be populated in Phase 2 when Mongoose models are defined.
export {};
